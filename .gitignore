runs/
*.step
*.stl
*.msh
*.db
*.parquet
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
config/mechanisms/*.yaml

# La cartella di quarantena: ci finisce quello che sta per essere buttato, e
# non ha senso versionarlo. Attenzione pero': ci sono dentro i quattordici zip
# datati da cui e' stata ricostruita la storia di questo repo. Una volta che il
# repo esiste, quelli sono ridondanti - ma finche' non si e' verificato che il
# push sia andato a buon fine, sono ancora l'unica copia.
(eliminare)/
