# Punto operativo di Zefiro. UNITA' INDICATE NEL NOME DEL CAMPO.
# I `null` sono TODO BLOCCANTI: il codice fallisce con MissingDatum elencandoli
# tutti insieme. NON sostituirli con valori plausibili: un default inventato
# produce numeri credibili e falsi. Riferimento: docs/architettura.md sezione 8.

p_amb_bar:          1.01325     # atmosfera standard al livello del mare

# --- IMPIANTO ARIA ----------------------------------------------------------
# [dato utente] compressore 3 cavalli, serbatoio 100 litri, max 10 bar.
#
# ATTENZIONE ALLA SEMANTICA DI QUESTO CAMPO. Non e' la pressione MASSIMA del
# serbatoio, e' la MINIMA utile a fine raffica. Motivo: il riduttore tiene p_c
# costante solo finche' a monte ha almeno p_c + Dp_iniettore + la sua caduta.
# L'istante critico e' la FINE della raffica, quando il serbatoio e' al minimo.
# Dimensionare su 10 bar significherebbe progettare per un istante che dura zero.
# 'derived' = uguale a p_sat(T_design) della bombola. NON e' una coincidenza
# imposta: e' il solo valore che non spreca nulla. Fermarsi piu' in alto butta
# via aria (e quindi durata) senza alzare p_c, che lo fissa comunque il GPL;
# scendere piu' in basso fa perdere il controllo al riduttore dell'aria PRIMA
# che a quello del GPL, e la miscela va ricca in modo incontrollato proprio a
# fine raffica, con il motore gia' caldo. Vedi feed.supply_pressures.
p_air_supply_bar:    derived

plant:
  compressor:
    shaft_power_hp:   3.0       # [dato utente] "3 cavalli"
    # TODO A: la portata di targa ("aria resa" / FAD, in l/min). E' il numero
    # che serve davvero. Sta sulla targhetta accanto ai giri e alla pressione.
    # Attenzione: alcuni costruttori dichiarano l'aria ASPIRATA (displacement),
    # che e' 1.3-1.6 volte l'aria resa. Serve l'aria RESA.
    fad_litres_per_min: null
    # Limite termodinamico superiore calcolabile dalla sola potenza:
    #   3 HP, 1 -> 10 bar, 20 C  =>  11.6 g/s con compressione isoterma ideale
    #                                 8.2 g/s con adiabatica monostadio ideale
    # Una macchina reale a pistoni sta ben sotto: attendersi 4-7 g/s.
  air_tank:
    volume_litres:    100.0     # [dato utente]
    p_max_bar:         10.0
    # Il serbatoio permette RAFFICHE a portata molto superiore alla continua:
    #   10 -> 6 bar rende 359 g utilizzabili (limite adiabatico)
    #   a 50 g/s -> 8.2 s ; a 100 g/s -> 3.8 s
    p_min_usable_bar:  derived  # = p_sat(T_design) = 7.32 bar a 15 C
    # Compromesso quantificato: scaricare piu' a fondo da' piu' portata (quindi
    # piu' spinta) ma abbassa la p_c ammissibile. La spinta cresce in modo
    # monotono scendendo, ma sotto i 6 bar mordono tre limiti pratici:
    #   - il riduttore ha bisogno di margine a monte per non perdere il controllo
    #   - la portata di GPL richiesta supera cio' che un riduttore da bombola eroga
    #   - eps scende sotto 1.2 e l'ugello smette di essere un ugello

# --- IMPIANTO GPL -----------------------------------------------------------
# NON scrivere qui un numero. La pressione di una bombola non e' un parametro
# di progetto: e' p_sat(T, composizione), e nient'altro. Non dipende da quanto
# liquido resta dentro. Il valore lo deriva zefiro.feed.supply_pressures dalla
# temperatura di progetto qui sotto.
#
# Perche' questo campo e' stato tolto: fino alla revisione 0.4.0 valeva 8.0 bar
# scritti a mano. Sono la tensione di vapore del propano puro a 18.3 C. Sotto
# quella temperatura la bombola NON eroga 8 bar, eroga meno, e il punto
# operativo dell'intero motore era valido solo per giornate tiepide senza che
# nulla nel codice lo dicesse. Ora e' impossibile scriverlo.
#
#   propano puro:  0 C -> 4.74 bar   10 C -> 6.37   15 C -> 7.32   20 C -> 8.36
#
# 'derived' = calcolato da (fuel.composition_mole, fuel.bottle.T_design_K).
# Un numero esplicito e' ancora ammesso ma viene RIFIUTATO se supera p_sat alla
# temperatura di progetto: serve solo a documentare una misura, non a inventare.
p_fuel_supply_bar:   derived

fuel:
  # TODO C: misura pressione E temperatura della bombola nello stesso momento,
  # a bombola in equilibrio termico (non subito dopo un prelievo), poi usa
  # zefiro.feed.composition_from_pressure per ricavare la frazione di propano.
  # Il valore qui sotto e' propano puro: PLACEHOLDER esplicito, non una misura.
  composition_mole:
    C3H8: 1.0
  # TODO D: prelievo gassoso o liquido. Una bombola da barbecue non ha pescante,
  # quindi in verticale eroga VAPORE. Con l'autorefrigerazione calcolata
  # (1 K in 10 s a 7.5 g/s) il vapore basta per le raffiche; il collo di
  # bottiglia e' semmai il riduttore.
  phase_at_injection: gas
  thermo_source: gri30.yaml
  bottle:
    # [DECISO] Temperatura MINIMA di progetto della bombola.
    # E' un requisito operativo, non una previsione meteo: sotto questa
    # temperatura il motore non deve essere acceso, e la si tiene con la
    # bombola immersa in un bagno d'acqua (un secchio ha una capacita' termica
    # enorme rispetto ai 30 W sottratti dall'evaporazione: vedi
    # feed.autorefrigeration).
    #
    # Perche' 15 C e non di piu'. La durata della raffica e' la massa d'aria
    # estraibile fra 10 bar e il fondo scarico, e il fondo scarico vale p_sat:
    # una bombola PIU' CALDA alza p_c e l'Isp ma accorcia la raffica, in fretta.
    #   bombola a 15 C -> p_c 6.36 bar, Isp 143.7 s, 7.00 s di raffica
    #   bombola a 20 C -> p_c 7.27 bar, Isp 147.9 s, 4.31 s  (requisito violato)
    # Il requisito dei 5 s cade esattamente a 18.8 C. 15 C lascia il 40 % di
    # margine sulla durata e resta una soglia che si puo' GARANTIRE dal basso,
    # cosa molto piu' facile che garantire un massimo.
    #
    # Perche' non di meno. Scendendo, eps -> 1.29 a 0 C e l'ugello smette di
    # essere un ugello; l'Isp perde 15 s. E il motore va costruito per UNA
    # geometria: la scelta e' la temperatura piu' bassa a cui si accende.
    T_design_K:       288.15
    tare_kg:          null      # TODO E: stampigliata sul collare
    total_weight_kg:  null      # TODO E: con una bilancia -> massa di liquido
    # TODO F: portata massima ammessa dal riduttore [kg/h]. Un riduttore da
    # barbecue standard eroga 30 mbar e ~1 kg/h: NON e' utilizzabile qui, dove
    # servono 5-8 bar. Serve un riduttore di alta pressione, e la sua portata
    # massima e' un vincolo hard sul punto operativo.
    regulator_max_kg_per_h: null

# --- Temperature a VALLE dei regolatori (TODO G) -----------------------------
# Non sono la temperatura ambiente: l'espansione attraverso il riduttore e'
# quasi isentalpica e raffredda il gas. Vanno misurate.
T_air_in_K:         null
T_fuel_in_K:        null

# --- PUNTO DI PROGETTO [DECISO]: 5 s in regime stazionario -------------------
# Il riduttore tiene p_c costante mentre il serbatoio scende da 10 bar fino al
# fondo scarico, che NON e' piu' 6 bar scelti a mano ma p_sat(T_design) = 7.32
# bar (vedi p_air_supply_bar). Massa utilizzabile 235 g, limite adiabatico.
#   mdot = 235.1 g / 5 s = 47.0 g/s
#
# QUESTO NUMERO E' SCESO del 35 % rispetto alla revisione precedente (71.8 g/s),
# e la ragione e' l'errore corretto in 0.4.0: il fondo scarico era stato scelto
# a 6 bar quando il tetto di p_c era ancora fissato da una pressione di bombola
# inventata. Legare il fondo scarico a p_sat toglie 124 g di aria utilizzabile.
# E' il costo di dire la verita' sulla bombola, e va contato, non nascosto.
#
# Cio' che NON cambia e' il motore da 50 N: assorbe 33.6 g/s, quindi sta dentro
# i 47.0 con il 40 % di margine, e la sua raffica dura 7.0 s invece di 5.0.
# Cio' che cambia e' il motore PIU' GRANDE alimentabile da questo impianto.
#
# La ricarica del compressore durante la raffica NON e' contata: e' ancora un
# TODO (A) e vale 4-7 g/s, cioe' il 3-5 % della massa. Contarla significherebbe
# far dipendere il punto di progetto da un numero che non hai ancora misurato.
# Quando leggerai la targa diventera' margine, non un aggiustamento.
mdot_air_max_kg_s:  0.0470

# --- TODO I: coefficienti di efflusso degli iniettori ------------------------
cd_injector_ox:     null
cd_injector_fuel:   null

# --- Conseguenza termica della scelta dei 5 s (vedi zefiro/thermal.py) -------
# Analisi con Bartz + parete transitoria 1D, proprieta' 316L PLACEHOLDER:
#   camera: q ~ 0.45 MW/m2 -> parete a 574 K dopo 5 s.  Nessun problema.
#           col radiativo dei gas (eps 0.05-0.30) sale al massimo a 800 K.
#   gola:   q ~ 4.7 MW/m2  -> parete a 1669 K dopo 5 s, cioe' alla FUSIONE.
#           raggiunge 900 C in 2.2 s.
# Ne segue che il film cooling iniettato in testa protegge una camera che non
# ne ha bisogno, e non arriva dove serve. f_film e' quindi messo a 0 nel
# progetto di default, e il raffreddamento va ripensato ALLA GOLA.

# --- [DECISO] ---------------------------------------------------------------
burn_time_s:        5.0
