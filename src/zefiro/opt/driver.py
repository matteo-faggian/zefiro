"""Driver di ottimizzazione (pymoo per il Pareto, BoTorch per il bayesiano). STUB."""
from __future__ import annotations


def run_nsga2(problem, pop_size: int, n_gen: int, seed: int):
    """NSGA-II sul modello L0 + surrogato. `seed` e' obbligatorio: senza, la
    run non e' riproducibile e non deve entrare nel database."""
    raise NotImplementedError("Fase 5 della ROADMAP.")
